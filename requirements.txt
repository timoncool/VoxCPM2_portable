# Основные зависимости VoxCPM (2.0.2+ для VoxCPM2, требует gradio>=6)
voxcpm>=2.0

# UI (Gradio 6.11+ имеет баг tabs freeze #13198 — пин на 6.10.0 — последняя стабильная)
gradio==6.11.0

# Аудио/IO
soundfile>=0.13.1
numpy>=1.21.0
aiofiles

# HuggingFace (для скачивания модели и voice pack)
huggingface_hub[hf_xet]

# Для LoRA-тренировки (training/scripts/train_voxcpm_finetune.py)
argbind
tensorboardX
safetensors
pyyaml
datasets<4  # для prepare_test_dataset.py (скачать тестовый датасет с HF)

# Авто-подготовка датасета из видео/аудио (Parakeet TDT 0.6B v3 INT8 ONNX)
# Сама модель (~670 MB) качается лениво при первом клике на «Авто-подготовку»
# [gpu,hub] — подтягивает onnxruntime-gpu и huggingface_hub автоматически
onnx-asr[gpu,hub]>=0.5
